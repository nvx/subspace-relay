name: subspace_relay_pb
description: Subspace Relay RFID relay project protobuf generated library
version: 1.0.3

repository: https://github.com/nvx/subspace-relay

environment:
  sdk: ^3.8.1

dependencies:
  protobuf: ^5.0.0
